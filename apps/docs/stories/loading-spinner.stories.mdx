import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { Box, ThemeProvider, LoadingSpinner } from '@mintuz/marvel'

<Meta title="Components/Loading Spinner" component={LoadingSpinner} />

# Loading Spinner

An SVG Icon that spins, scalable via parent components font size

<Preview>
    <Story name="Default">
        <ThemeProvider>
            <Box fontSize="64px" color="black">
                <LoadingSpinner />
            </Box>
        </ThemeProvider>
    </Story>
</Preview>

import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { Phone } from 'styled-icons/material'
import { ThemeProvider, Stepper } from '@mintuz/marvel'

<Meta title="4. Form/Stepper" component={Stepper} />

# Stepper

Allow a user to increase or decrease a value by an amount

## Props

<Props of={Stepper} />

## Example

<Preview>
    <Story name="Default">
        <ThemeProvider>
            <Stepper incrementBy={10} label={'Number of burgers'} value={20} />
        </ThemeProvider>
    </Story>
</Preview>

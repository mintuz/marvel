import styled from 'styled-components'
import {
    space,
    typography,
    flexbox,
    layout,
    color,
    border,
    position,
} from 'styled-system'

export default styled.div`
    ${layout}
    ${flexbox}
    ${position}
    ${space}
    ${typography}
    ${color}
    ${border}
`

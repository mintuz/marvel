import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { Badge, ThemeProvider } from '../src'

<Meta title="3. Components|Badge" component={Badge} />

# Badge

The Badge component is used to bring attention something to a user.

## Example

<Preview>
    <Story name="Default">
        <ThemeProvider>
            <Badge variant="primary">My badge</Badge>
        </ThemeProvider>
    </Story>
</Preview>

## Props

<Props of={Badge} />

import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { ThemeProvider, Typography } from '../src'

<Meta title="1. Foundations|Typography" component={Typography} />

# Typography

## Example

<Preview>
    <Story name="Default">
        <ThemeProvider>
            <div>
                <Typography as="h1" mb={[0]} mt={[0]} variant="displayOne">
                    Display One
                </Typography>
                <Typography as="h2" mb={[0]} mt={[0]} variant="displayTwo">
                    Display Two
                </Typography>
                <Typography as="h3" mb={[0]} mt={[0]} variant="displayThree">
                    Display Three
                </Typography>
                <Typography mb={[0]} mt={[0]} variant="body">
                    Body
                </Typography>
            </div>
        </ThemeProvider>
    </Story>
</Preview>

## Props

<Props of={Typography} />
